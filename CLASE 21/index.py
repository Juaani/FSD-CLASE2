def saludar():
    """Devuelve siempre 1"""
    x=1

entrada = int(input("Mensaje: ")) #entrada por teclado

entrada = list(input("Mensaje: ")) #entrada de lista

entrada = tuple(input("Mensaje: ")) #lista no modificable

x, y, z = [1,2,3] #asignar datos
data = []
while True:
    line = int(input("mensaje: "))
    if line:
        data.append(int(line))
    else:
        break

#if else determinado por la identacion no por el ;

def saludo(nombre):
    print(f"Hola {nombre}")

#funciones

